# This file contains the image's default configuration for `initramfs arguments` used by the `initramfs-setup` BlueBuild module.
# This config uses the file location format, with one file location per line.

