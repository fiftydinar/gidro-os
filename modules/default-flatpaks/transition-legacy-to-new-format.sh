#!/usr/bin/env bash

# Tell build process to exit if there are any errors.
set -euo pipefail

# Script, which is should be sourced, if conditions are correct, for transforming existing installs config to new YAML one
