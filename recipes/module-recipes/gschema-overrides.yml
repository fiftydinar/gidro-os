modules:
  - type: script
    snippets:
      # Workaround any mutter experimental features not applying, like VRR
      - rm /usr/share/glib-2.0/org.gnome.mutter.fedora.gschema.override

  - type: gschema-overrides
